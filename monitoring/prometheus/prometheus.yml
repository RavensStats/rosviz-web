global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: 'robot_alert_monitor'
    static_configs:
      - targets: ['ros-stack:8888']
